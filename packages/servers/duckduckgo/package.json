{
  "name": "@mcp/server-duckduckgo",
  "version": "1.0.0",
  "description": "DuckDuckGo MCP Server",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsup src/index.ts --watch --format esm --dts",
    "build": "tsup src/index.ts --format esm --dts",
    "lint": "eslint src --ext .ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@mcp/server-base": "workspace:*",
    "@mcp/shared": "workspace:*",
    "node-fetch": "^3.3.2"
  }
}

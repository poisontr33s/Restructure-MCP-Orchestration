{
  "name": "@mcp/server-m365-graph",
  "version": "1.0.0",
  "private": true,
  "description": "Microsoft 365 Graph API MCP Server Implementation",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "dev": "tsup src/index.ts --format esm --dts --watch",
    "lint": "eslint src --ext .ts",
    "test": "vitest run"
  },
  "keywords": [
    "mcp",
    "microsoft-365",
    "graph-api",
    "server"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@mcp/server-base": "workspace:*",
    "@mcp/shared": "workspace:*",
    "@mcp/agent-m365-graph": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "tsup": "^8.0.2",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
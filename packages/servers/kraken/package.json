{
  "name": "@mcp/kraken",
  "version": "1.0.0",
  "description": "The Kraken server for parallel processing",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsup src/index.ts --watch --format esm --dts",
    "build": "tsup src/index.ts --format esm --dts",
    "lint": "eslint src --ext .ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@mcp/shared": "workspace:*"
  }
}
